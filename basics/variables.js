// 1. you can't define a variable more than once
let petName = 'Hal'
petName = 'Spot'

console.log(petName)

// 2. There are rules releated to variable names (letters, $ or _)
let test_ = 2
let result = 3 + 4

// 3. Variables names can not be reserved keywords
// let let = 12